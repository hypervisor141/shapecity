design custom separate collision analyzers

ray casting

bounding loader unprojection

color picking

change the input method functionality, where events are handled in render loop as
    tasks, and mouse inputs are queued (potential issues with timing and queuing)